# Concrete Block Analysis Pipeline v3 - Requirements
# For: carbonation_analyzer_debug.py, main_v3_debug.py
# Python 3.8+

# ============================================================
# CORE COMPUTER VISION
# ============================================================
opencv-python>=4.8.0
numpy>=1.24.0,<2.0
scipy>=1.10.0
scikit-learn>=1.3.0
scikit-image>=0.21.0
Pillow>=10.0.0

# ============================================================
# GPU DEEP LEARNING (PyTorch)
# ============================================================
# Choose ONE installation method below:

# Option 1: NVIDIA GPU with CUDA 11.8 (RECOMMENDED)
# pip install -r requirements.txt
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Option 2: NVIDIA GPU with CUDA 12.1
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Option 3: Apple Silicon GPU (MPS)
# pip install torch torchvision

# Option 4: CPU only (No GPU)
# pip install torch torchvision

# Generic PyTorch (auto-detects your system)
torch>=2.0.0
torchvision>=0.15.0

# ============================================================
# SEGMENTATION & OBJECT DETECTION
# ============================================================
ultralytics>=8.0.0  # SAM2 model and YOLOv8

# ============================================================
# IMAGE PROCESSING & ALGORITHMS
# ============================================================
# (already included via scikit-image and opencv)

# ============================================================
# UTILITIES & LOGGING
# ============================================================
tqdm>=4.66.0
matplotlib>=3.7.0
pandas>=2.0.0

# ============================================================
# OPTIONAL: OCR for Ruler Text Detection
# ============================================================
easyocr>=1.6.0
paddleocr>=2.7.0

# ============================================================
# OPTIONAL: Data Augmentation
# ============================================================
albumentations>=1.3.0

# ============================================================
# INSTALLATION INSTRUCTIONS
# ============================================================
# 
# 1. Basic Installation (CPU):
#    pip install -r requirements.txt
#
# 2. GPU Installation (NVIDIA - Recommended):
#    pip install -r requirements.txt
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 3. GPU Installation (Apple Silicon):
#    pip install -r requirements.txt
#    (PyTorch will auto-detect MPS support)
#
# 4. Verify Installation:
#    python -c "import torch; print(f'GPU: {torch.cuda.is_available()}')"
#    python -c "from carbonation_analyzer_debug import CarbonationAnalyzer; print('✓ All modules ready')"
#
# ============================================================
